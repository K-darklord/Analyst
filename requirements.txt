.

# FORK EXTENSION — fork-specific deps for the K-darklord fork.
# Install with: pip install -r requirements.txt
# (or use: pip install -e ".[fork]" which is the canonical install path)
tushare>=1.4.0
pyyaml>=6.0
akshare>=1.12.0

# xfinance: multi-source failover for Yahoo Finance (US market_data/fundamentals/news)
xfinance>=0.4.1
tornado>=6.0   # required by pybreaker.call_async used by xfinance's circuit breaker
